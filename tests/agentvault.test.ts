/**
 * Anchor integration tests run via `anchor test` (configured in Anchor.toml).
 * Skeleton only — fill in per docs/01_SOLANA_PROGRAM.md §6 (test plan).
 */
import * as anchor from "@coral-xyz/anchor";

describe("agentvault", () => {
  it("initializes", async () => {
    anchor.setProvider(anchor.AnchorProvider.env());
    // TODO
  });
});
